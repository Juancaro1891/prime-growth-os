import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Términos de Servicio — PRIME GROWTH OS",
  description: "Condiciones de uso de PRIME GROWTH OS, incluyendo la integración con tu cuenta de Meta Ads.",
}

export default function TermsLayout({ children }: { children: React.ReactNode }) {
  return children
}
